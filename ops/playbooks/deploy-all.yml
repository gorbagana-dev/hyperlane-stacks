---
# Phase 2 — deploy stacks (assumes setup-all has run). Stops after stacks are
# running; on-chain ownership/ISM is sub-project 3. Each singleton play passes only
# stack_name; stack_deploy resolves spec/path/is_job from the `stacks` map in
# group_vars and assembles the secret env from `stack_env_vars`.
- name: Preflight — fleet is provisioned
  hosts: all:!controller
  gather_facts: false
  tasks:
    - name: Verify laconic-so is present (else run setup-all first)
      ansible.builtin.command: laconic-so version
      changed_when: false

- name: MinIO
  hosts: minio_hosts
  gather_facts: true
  vars_files:
    - "{{ inventory_dir }}/secrets.yml"
  vars:
    stack_name: hyperlane-minio
  pre_tasks:
    - name: Derive validator labels + MINIO_USERS
      ansible.builtin.include_tasks: ../roles/common/tasks/load_validators.yml
    - name: Build MinIO env (MINIO_USERS + per-validator IAM)
      ansible.builtin.set_fact:
        stack_env_extra: >-
          {{ stack_env_extra | default({'MINIO_USERS': minio_users}) | combine({
             (item.label | upper | replace('-', '_') ~ '_KEY_ID'): minio_iam[item.label].key_id,
             (item.label | upper | replace('-', '_') ~ '_SECRET'): minio_iam[item.label].secret }) }}
      loop: "{{ validators }}"
      loop_control:
        label: "{{ item.label }}"
      no_log: true
  roles:
    - stack_deploy

- name: Deployer (Job)
  hosts: deployer_hosts
  gather_facts: true
  vars_files:
    - "{{ inventory_dir }}/secrets.yml"
  vars:
    stack_name: hyperlane-svm-deployer
  roles:
    - stack_deploy

- name: Publish deployer-produced bridge state
  ansible.builtin.import_playbook: publish-bridge-state.yml

- name: Relayer
  hosts: relayer_hosts
  gather_facts: true
  vars_files:
    - "{{ inventory_dir }}/secrets.yml"
  vars:
    stack_name: hyperlane-relayer
    configmap_names: "{{ stacks['hyperlane-relayer'].configmaps }}"
  roles:
    - state_distribute
    - stack_deploy

- name: Gas oracle
  hosts: gas_oracle_hosts
  gather_facts: true
  vars_files:
    - "{{ inventory_dir }}/secrets.yml"
  vars:
    stack_name: hyperlane-gas-oracle
  roles:
    - stack_deploy

- name: Monitoring
  hosts: monitoring_hosts
  gather_facts: true
  vars_files:
    - "{{ inventory_dir }}/secrets.yml"
  vars:
    stack_name: hyperlane-monitoring
  roles:
    - stack_deploy

- name: Warp UI
  hosts: warp_ui_hosts
  gather_facts: true
  vars_files:
    - "{{ inventory_dir }}/secrets.yml"
  vars:
    stack_name: hyperlane-warp-ui
  roles:
    - stack_deploy

# Validators: looped over validators.yaml, each on its own host. state_distribute
# then stack_deploy, delegated to the validator's host. Spec is chosen per chain
# (spec-validator-<chain>.yml); deploy_dir is home-based on the target host.
# NOTE (VM-verify): delegate_to + ansible_env/fact behavior for multi-host
# validators needs confirmation on a real multi-VM run; correct for single-host v1.
- name: Validators
  hosts: controller
  gather_facts: false
  vars_files:
    - "{{ inventory_dir }}/secrets.yml"
  tasks:
    - name: Load validators
      ansible.builtin.include_tasks: ../roles/common/tasks/load_validators.yml

    - name: Distribute state to each validator host
      ansible.builtin.include_role:
        name: state_distribute
        apply:
          delegate_to: "{{ item.host }}"
      vars:
        stack_name: "hyperlane-validator-{{ item.label }}"
        deploy_dir: "/home/{{ hostvars[item.host].deploy_user }}/deployments/hyperlane-validator-{{ item.label }}"
        configmap_names: "{{ stacks['hyperlane-validator'].configmaps }}"
      loop: "{{ validators }}"
      loop_control:
        label: "{{ item.label }}"

    - name: Deploy each validator on its host
      ansible.builtin.include_role:
        name: stack_deploy
        apply:
          delegate_to: "{{ item.host }}"
      vars:
        stack_name: "hyperlane-validator-{{ item.label }}"
        spec_file: "{{ deployment_root }}/spec-validator-{{ item.chain }}.yml"
        stack_path: "{{ repo_root }}/{{ stacks['hyperlane-validator'].path }}"
        deploy_dir: "/home/{{ hostvars[item.host].deploy_user }}/deployments/hyperlane-validator-{{ item.label }}"
        stack_env_map_key: hyperlane-validator
        stack_env_extra: >-
          {{ {(item.label | upper | replace('-', '_') ~ '_KEY_ID'): minio_iam[item.label].key_id,
              (item.label | upper | replace('-', '_') ~ '_SECRET'): minio_iam[item.label].secret} }}
      loop: "{{ validators }}"
      loop_control:
        label: "{{ item.label }}"
