---
# Reads generated state from the on-host clone fetch_stack maintains
# ({{ deployment_root }}/bridges/{{ bridge_name }}/generated). No own repo clone.
# Caller sets: deploy_dir, configmap_names (list). repo_root/deployment_root come
# from group_vars (or per-host overrides for delegated validators).
