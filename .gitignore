__pycache__/
node_modules/
.data/
.deployments
.logs
.keys
.venv/
.backpack-ext/
ops/collections/

# ops: per-env operator config + generated secrets (never commit).
# secrets.yml is the pre-rename name — keep ignoring operators' existing files.
ops/inventories/*/deployment-config.yml
ops/inventories/*/secrets.yml
