--- a/rust/main/hyperlane-base/src/settings/signers.rs	2026-03-11 10:51:08.488847042 +0000
+++ b/rust/main/hyperlane-base/src/settings/signers.rs	2026-03-11 10:51:28.296041291 +0000
@@ -96,11 +96,21 @@
                 ),
             )),
             SignerConf::Aws { id, region } => {
+                // Support AWS_ENDPOINT_URL_KMS for custom KMS endpoints (e.g. local proxy).
+                // rusoto_core::Region::from_str only produces named variants and cannot
+                // express a custom endpoint, so we check the env var and wrap the region.
+                let effective_region = match std::env::var("AWS_ENDPOINT_URL_KMS") {
+                    Ok(endpoint) => Region::Custom {
+                        name: region.name().to_owned(),
+                        endpoint,
+                    },
+                    Err(_) => region.clone(),
+                };
                 let http_client = utils::http_client_with_timeout()
                     .map_err(|err| eyre::eyre!(err.to_string()))?;
                 let client = KmsClient::new_with_client(
                     rusoto_core::Client::new_with(AwsChainCredentialsProvider::new(), http_client),
-                    region.clone(),
+                    effective_region,
                 );
                 let signer = AwsSigner::new(client, id, 0, Some(AWS_SIGNER_TIMEOUT)).await?;
                 hyperlane_ethereum::Signers::Aws(signer)
@@ -138,11 +148,18 @@
                 Ok(hyperlane_tron::TronSigner::from(wallet))
             }
             SignerConf::Aws { id, region } => {
+                let effective_region = match std::env::var("AWS_ENDPOINT_URL_KMS") {
+                    Ok(endpoint) => Region::Custom {
+                        name: region.name().to_owned(),
+                        endpoint,
+                    },
+                    Err(_) => region.clone(),
+                };
                 let http_client = utils::http_client_with_timeout()
                     .map_err(|err| eyre::eyre!(err.to_string()))?;
                 let client = KmsClient::new_with_client(
                     rusoto_core::Client::new_with(AwsChainCredentialsProvider::new(), http_client),
-                    region.clone(),
+                    effective_region,
                 );
                 let signer = AwsSigner::new(client, id, 0, Some(AWS_SIGNER_TIMEOUT)).await?;
                 Ok(hyperlane_tron::TronSigner::Aws(signer))
