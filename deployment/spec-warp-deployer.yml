# Hyperlane SVM Warp Route Deployer - deployment spec
# Deploys warp route contracts for a specific token pair.
# Requires core deployer (spec-deployer.yml) to have run first.
#
# Set all vars explicitly — SO k8s path doesn't support nested
# ${VAR:-${OTHER}} defaults from compose files.
stack: stack_orchestrator/data/stacks/hyperlane-svm-warp-deployer
deploy-to: k8s-kind
namespace: laconic-hyperlane
kind-cluster-name: hyperlane
config:
  WARP_TOKEN_MINT: "REPLACE_WITH_TOKEN_MINT_ADDRESS"
  WARP_ROUTE_NAME: "USDC-solana-gorchain"
  COLLATERAL_CHAIN: solana
  SYNTHETIC_CHAIN: gorchain
  GORCHAIN_RPC_URL: "REPLACE_WITH_GORCHAIN_RPC_URL"
  GORCHAIN_DOMAIN_ID: "99999"
  GORCHAIN_CHAIN_ID: "99999"
  SOLANA_RPC_URL: "REPLACE_WITH_SOLANA_RPC_URL"
  SOLANA_DOMAIN_ID: "99998"
  SOLANA_CHAIN_ID: "99998"
  GORCHAIN_IS_TESTNET: "false"
  SOLANA_IS_TESTNET: "false"
  # Derived vars (collateral=solana, synthetic=gorchain by default)
  COLLATERAL_CHAIN_RPC_URL: "REPLACE_WITH_SOLANA_RPC_URL"
  COLLATERAL_DOMAIN_ID: "99998"
  SYNTHETIC_CHAIN_RPC_URL: "REPLACE_WITH_GORCHAIN_RPC_URL"
  SYNTHETIC_DOMAIN_ID: "99999"
  # Synthetic token metadata URI — required when IS_TESTNET=false.
  # Must point to JSON with matching name/symbol, description, and image URL.
  # Example: https://raw.githubusercontent.com/.../metadata.json
  WARP_TOKEN_METADATA_URI: "REPLACE_WITH_TOKEN_METADATA_URI"
  FORCE_REDEPLOY: "false"
configmaps:
  warp-deployer-scripts-config: ./configmaps/warp-deployer-scripts-config
  warp-deployer-token-config: ./configmaps/warp-deployer-token-config
  warp-deployer-registry-config: ./configmaps/warp-deployer-registry-config
secrets:
  hyperlane-warp-deployer-secrets:
    - DEPLOYER_KEYPAIR
    - HARDWARE_WALLET_PUBKEY
# Private registry auth — set GHCR_PAT env var before deploy start:
#   export GHCR_PAT=ghp_xxxx  # GitHub PAT with packages:read scope
registry-credentials:
  server: ghcr.io
  username: REPLACE_WITH_GITHUB_USERNAME
  token-env: GHCR_PAT
