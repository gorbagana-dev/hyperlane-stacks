# Warp route: USDC (Solana collateral) <-> gorchain synthetic.
# Chain endpoints live in spec-warp-deployer.yml, not here.
name: USDC-solana-gorchain
origin:
  chain: solana
  type: collateral
  # Runtime-generated SPL mint, substituted by load_warp_routes.yml from
  # {{ credentials_dir }}/warp-token-mint (single-host: prepare-chains writes it;
  # multi-host: drop the deploy-spl-token output there alongside the keys).
  token: "__WARP_TOKEN_MINT__"
  name: "USD Coin"
  symbol: "USDC"
  decimals: 6
remote:
  chain: gorchain
  type: synthetic
  name: "USD Coin"
  symbol: "USDC"
  decimals: 6
metadataUri: ""
